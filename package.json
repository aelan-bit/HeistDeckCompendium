{
  "name": "heist-deck-compendium",
  "version": "1.0.0",
  "description": "Heist Deck - Blades in the Dark — Foundry VTT module build scripts",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-sources.mjs",
    "compile": "node scripts/pack.mjs",
    "build": "npm run generate && npm run compile"
  },
  "dependencies": {
    "classic-level": "^1.4.1"
  }
}
